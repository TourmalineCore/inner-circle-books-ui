import './ScanContent.scss'

import { observer } from "mobx-react-lite"

export const ScanContent = observer(() => {
  
  return (
    <div 
      className="scan"
      data-cy="scan"
    >
    </div>
  )
})
